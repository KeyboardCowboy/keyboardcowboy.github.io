ul.posts
  | {% for post in site.posts %}
  li
    span.post--meta
      | {{ post.date | date: "%b %-d, %Y" }}
    span.post--title
      a(href="{{ post.url | prepend: site.baseurl }}") {{post.title}}
  | {% endfor %}
