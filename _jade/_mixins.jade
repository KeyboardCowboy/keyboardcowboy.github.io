mixin social_link(icon, text, href)
  a(href="#{href}", target='_blank')
    i(class="#{icon}")
    span #{text}

mixin nav(id)
  nav(id="nav-#{id}", role="navigation")
    block

mixin submenu
  .submenu
    block
    .spacer
